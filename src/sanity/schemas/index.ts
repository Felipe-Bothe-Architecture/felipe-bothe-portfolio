import about from "./about-schema";
import contact from "./contact-schema";
import hero from "./hero-schema";
import project from "./project-schema";

const schemas = [hero, about, project, contact];

export default schemas;